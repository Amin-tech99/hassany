[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/"
healthcheckTimeout = 100

[phases.setup]
cmds = [
  "apt-get update",
  "apt-get install -y software-properties-common ffmpeg python3 python3-dev python3-pip",
  "python3 -m pip install --upgrade pip setuptools wheel"
]

[phases.install]
cmds = [
  "npm config set registry https://registry.npmjs.org/",
  "npm config set strict-ssl false",
  "npm config set legacy-peer-deps true",
  "npm install --no-audit --no-fund --prefer-offline --legacy-peer-deps --force",
  "pip3 install torch torchaudio numpy --extra-index-url https://download.pytorch.org/whl/cpu",
  "pip3 install -r requirements.txt"
]

[phases.build]
cmds = [
  "npm run build"
]

[mounts]
source = "/data"
destination = "/app/uploads"